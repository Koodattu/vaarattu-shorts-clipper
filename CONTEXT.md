# Current project state

Latest selection/chat follow-up: conversation-v5 treats a clear subject/hook within roughly two seconds and a resolved payoff as preferences, not requirements, per creator correction. Interesting discussion with a gradual opening or open-ended subject can qualify. Only standalone, substance and fidelity must reach 3; opening/payoff remain ranking signals with no minimum total-score gate. Automatic cuts already allow at most 200 ms before the first selected word, meeting the requested 0.5s onset by ASR timing. Preserve natural speech and necessary qualifications; do not over-trim to force a hook or extend merely for closure. Verifier can trim filler by advancing the coarse idea anchor within the SAME original passage, not jump to a new idea passage. API streams list and streams/254/activity now confirmed live HTTP200 (188 two-minute buckets); earlier missing-route notes superseded. Old integration only boosted ranking after selection. New selection snapshots chat before LLM, full scan unchanged, separate peak-region discovery, merge/deduplicate, blind verification and no chat score boost. Requires existing explicit stream-ID/offset confirmation; title match alone remains insufficient. Handles unavailable data/peak-specific failures separately from full-transcript coverage. Saves chat-input.json and chat-peak-review.json, source provenance and run UI summary. Prior Codex changes remain intact. No app restart, inference or video processing performed; public API reads only. See docs/stream-data.md and docs/selection-design.md.

Latest provider addition (2026-09-06): Codex via user's already-running openai-api-server-via-codex at http://127.0.0.1:18080/v1. New UI provider codex, default gpt-5.6-luna, both passes low reasoning. .env CODEX_BASE_URL/CODEX_MODEL/CODEX_API_KEY added preserving existing entries; optional local bridge key never uses OPENAI_API_KEY. Endpoint/model snapshotted per run and included in cache identity. Responses native JSON schema, store=false, 300s timeout, no paid fallback. Published bridge removes max_output_tokens; adapter omits it and records no enforceable output cap. Usage tokens recorded; public money/rates/cap null with codex_subscription basis, UI dollar cap disabled. See docs/codex-provider.md. No bridge/app startup, account login, inference, video processing or dependency installation performed. Juha still exclusively starts/restarts the app.

Updated 2026-09-06 after end-to-end single-video implementation and the Turbo-only STT correction.

## User intent and confirmed choices

Create a fresh Vaarattu-only tool to find interesting spoken Finnish conversations/opinions/stories in YouTube VODs. Initially select one video and press Run: the whole pipeline automatically produces clips in the output directory. Admit only one queued/active VOD; no automatic next video. Newest-first archive processing comes later. Full audio first; download only selected video ranges. Compose camera above gameplay, add captions and automatically create ready-to-post files. Juha handles publishing. Process on this Windows PC; local UI now, private remote access later. Other projects, especially highlight-clipper, stay separate.

## Current status

The application code, local UI, worker, model preparation commands and offline tests now exist. Base/test dependencies were installed in .venv and uv.lock was generated. The ASR extra, model weights and llama.cpp runtime were not installed/downloaded. No application server, processing run, real media acquisition, inference, render, scheduled job or public API change was performed. Work remains uncommitted. See docs/implementation-status.md for exact scope and deferred features.

Read [README.md](README.md) for the document map and [docs/implementation-plan.md](docs/implementation-plan.md) for ordered milestones. [docs/decisions.md](docs/decisions.md) records decisions, alternatives and unknowns; update it when a choice changes.

Verification: 25 offline tests passed, Ruff lint/format and JavaScript syntax passed, sdist/wheel built offline and required UI/ASR modules were checked in the wheel. Twelve docs/44 local links passed validation. Two third-party test-client deprecation warnings remain. No live runtime or quality gate is claimed.

## Recommended implementation

Python 3.12 + uv; FastAPI with static HTML/CSS/JS; one durable Python worker; SQLite and local artifacts. yt-dlp and FFmpeg. Only faster-whisper Turbo STT is implemented, per the latest user instruction. Other ASR remains research. All model weights and model-library caches stay inside this project under .cache/, with actual weights in .cache/models/. Use Gemma 4 via llama.cpp, testing 31B Q4 first and 26B-A4B Q4 alongside it. Aim for the largest useful fully GPU-resident LLM at tested context, not a small model sharing memory with ASR. Owned ASR process exits before LLM load; LLM exits before any caption-refinement ASR reload. One slot, 16K initial fit test, 32K only after measurement; MTP disabled for the baseline.

Gemini 3.8 Flash, GPT-5.6 Luna, GLM-5.3-Flash and DeepSeek V4 Flash adapters are implemented with fixture checks. Meta Muse Spark 1.3 remains visibly disabled pending a verified API contract. Configure only the selected provider's key; local mode remains independent. Meta release availability is verified, but primary API/pricing documentation was login-gated; its exact contract remains unverified. Current prices, promotion expiries and provider-specific reasoning behavior are in [docs/models-and-providers.md](docs/models-and-providers.md). No Finnish winner, hardware fit or API behavior has been benchmarked. This supersedes the initial Qwen3.5-9B/Gemini 3.5 Flash-Lite recommendation. No embedding/vector DB or generic multi-agent pipeline initially.

Scan the full timed transcript with overlapping context, return source word/segment IDs, verify original surrounding speech, and preserve contiguous source chronology. Chat is optional, with separately confirmed identity and offset. Known layout presets allow automatic output; uncertain layout/alignment/meaning goes to attention. Normal export does not require user approval.

## Verified facts to retain

- PC: i7-13700K, about 64 GB RAM, RTX 4090 24 GB. C: had about 356.5 GiB free; E: about 3,868.2 GiB. Neither throughput nor exact runtime compatibility has been measured.
- Verified YouTube channel ID: `UCUCV40VqBZqt83afjbbICvw`.
- Sample VOD `OJ-bDXbfEos`: title date 2026-07-09, upload 2026-07-16, hydrated duration 22,440 seconds. Flat duration differed by one second.
- Public stream list reported 199 streams. Likely identity match: stream 254, July 9 14:51:00Z to 21:05:31.793Z. Duration difference 31.793 seconds is not a measured start offset.
- vaarattu.tv local source commit `96a2afa` contains activity API; live `/api/streams/277/activity` returned `Route not found`. Matching/list API works; activity deployment remains separate work.
- Current aggregate `activeChatters` is peak minute-distinct chatters in widened chart buckets. It is not whole-bucket uniqueness. Collector timestamps are receipt time. Unknown coverage must not become known zero.
- highlight-clipper's own status says quality promotion is pending. Reuse small timestamp/caption/recovery techniques; do not claim its smoke tests prove clip quality or inherit its architecture.

## Next action

Do not start the product until Juha requests a run. When authorized, follow docs/setup.md to install the optional ASR runtime, configure tools, explicitly prepare Turbo/Gemma, calibrate one source layout and validate a single actual VOD end-to-end. Record fit, timing, output quality and actual costs before backlog work. Models stay project-local; output currently uses workdir/ready. Meta, other ASR, cache eviction, arbitrary output roots and backlog/scheduling remain deferred as documented.

The 2026-09-06 setup/usage follow-up added a durable request token/rate ledger, UI totals and JSON usage downloads. Reported truncated/invalid response usage is settled before content validation; missing usage keeps reservations. Conservative costs do not apply cache/off-peak discounts or reconcile invoices. Local requests record zero API cost. 35 offline tests passed. No app, model, media or paid API was run. Setup docs now include session-only key entry and the remaining Windows GPU runtime prerequisites.

The subsequent secrets-file request added automatic project-root `.env` loading to CLI settings initialization, a blank ignored `.env`, and tracked `.env.example`. Existing environment variables win. The dependency-free parser supports single-line literal values, quotes and comments, without expansion or execution. Setup docs now prefer `.env`. 41 offline tests passed; no app or pipeline was started.

The channel-browser follow-up adds `YOUTUBE_API_KEY` and `YOUTUBE_CHANNEL_ID` in `.env`, uploads-playlist pages through the existing HTTP client, SQLite catalog/cursors and a searchable newest-first UI with selection and processed badges derived from all run history. Explicit latest/older buttons use API quota; page reload/polling uses saved data. Runs snapshot channel identity; legacy runs stay on VaarattuVODs. No automatic queue, live API call or media/model run was added to the implementation session. See docs/setup.md for the browser workflow and refresh semantics.

User then started a real run. Read-only NVIDIA/process/module checks confirmed unbatched Turbo on the RTX 4090, FP16, with CUDA 12 cuBLAS and cuDNN 9 loaded and completed chunks. The subsequent speed request implements batch size 16 by default for NEW runs, matching niilo22; TOML settings asr_batch_size (0=legacy, 1–64=batched) and asr_flash_attention (default false) are snapshotted at admission. Existing runs keep unbatched settings/fingerprints. New ASR outputs record runtime/throughput. 68 offline Python tests and the JS UI test passed. No restart, interruption, extra live inference or batching benchmark was performed. Restart after the active run completes to use batching for a new run.

The first run a10dd4555f3142fc938bc9035607285a then failed in transcript merging: chunks 10/11 overlapped 220 ms at source second 13200. Implemented conservative three-word/time-matched context handoff; no retiming or relaxed overlap threshold. 72 offline tests passed. Repaired the actual transcript/checkpoint from all 19 cached chunks (20757 words), verified raw JSON hashes unchanged, with subprocesses prohibited, and resumed the same existing OpenAI run with its original $1 cap. Recovery script/audit are in ignored .cache/repair-transcript.py and that run's asr/seam-repair.json. No restart was needed for this repaired checkpoint; the long-lived worker still uses its originally loaded code for new merges until restarted.

No paid inference or automatic publishing is authorized/configured by this documentation. Do not run migration/deployment commands merely because they appear in reference documents.


Selection efficiency follow-up (2026-09-06): implemented passages-v2. Canonical Whisper word timings remain untouched. Deterministic original-text passages use punctuation/pauses plus 15-second/45-word bounds. Six-minute discovery cores retain 90 seconds of context; API input budgets are independent of local 16K/32K allocation. Native schema is no longer repeated in prompt text. Verification hides discovery scores/reasons, shows individual word IDs only around candidate +/-15 seconds, uses OpenAI low reasoning (discovery none), and can expand context from 90 to 180 seconds only if new words exist. Source/ownership checks participate in the one allowed paid repair. Selection-only chain version and prompt cache hashes prevent stale inference reuse without retranscribing. Existing run a10dd4555f3142fc938bc9035607285a is paused; RESTART APP BEFORE RESUME so the old worker cannot continue its imported 1,111-window code. Old spending/artifacts are preserved. Offline replay measured 63 discovery calls vs 1,111; all 20,757 words preserved; aggregate text 628,243 vs 3,796,649 bytes. 84 offline Python tests passed; completed selection checkpoints preserve their version/clip identity while unfinished selection upgrades. No paid generation, output-quality benchmark or app restart performed. See docs/selection-design.md and docs/selection-efficiency.json.


Selection resilience follow-up: user rejected whole-VOD failure on an out-of-section idea. Fixed discovery to validate/filter candidates individually and preserve valid siblings, with durable selection-issues.json audit. ModelOutputError distinguishes exhausted model-content repairs from operational errors. Unreadable windows continue as explicit partial coverage; bad verifier output makes only that candidate ineligible. Pipeline reports skipped counts and partial/needs_attention while exporting valid clips; channel badges do not treat partial as complete, rerenders retain warnings. 89 Python tests + JS UI test passed, lint/format/syntax passed. Existing failed response for discovery-5040000000-5400000000 was cached from its saved paid response (fingerprint 443de4b4da5652d58a03785f71ed2f60a4588e76d2fe37b4aeac89b1c8abe29d), preserving raw hashes/accounting. No v2 prompt/cache version bump. Restarted verified project launcher only (old base PID81944, new wrapper PID93532, hidden), resumed run a10dd4555f3142fc938bc9035607285a using local API under original $1 cap. Earlier paused/restart-required notes are superseded.


Latest reasoning preference: OpenAI Luna discovery now explicitly uses low, matching verification. This supersedes earlier none-discovery notes. User requested this while the first VOD was already verifying at low; preserve its completed none discovery rather than rerun it. Request hashes already distinguish effort; accounting records actual effort and reasoning tokens. Restart the worker after this selection checkpoint is saved to activate low discovery for subsequent VODs.

Low-reasoning activation completed: 33 relevant offline tests passed with lint/format checks. The first VOD completed its selection using existing none discovery plus low verification; actual verification responses reported reasoning tokens. After the selection checkpoint was saved, paused rendering, restarted only the verified project launcher, and resumed the same run. New worker loads low discovery for future VODs; no completed discovery was repeated.


User explicitly corrected runtime ownership: DO NOT start or restart the app; Juha runs it himself. At his request stopped the assistant-created background launcher (wrapper PID95928 and its verified serve child). Verified launcher and worker locks free. Duplicate serve now prints already-running URL instead of a lock-contention traceback, while real file-access failures remain distinct. 3 focused offline launcher tests passed. Earlier permissions to automatically restart/resume are superseded by this instruction.

Clip fixes follow-up: conversation-v3 targets general-audience stories/takes/jokes, usually 15–45s, automatic 5–60s, with no proposal/verification/export count quotas. Existing completed selection is preserved; manual edits/old clips allow up to 90s. First VOD had 34 proposals/10 verified/2 eligible, both held before render. Source video began 2.566/4.451s after near-zero audio. Section downloads now keyframe-safe H.264/AAC Matroska encodes. Alignment decodes section PCM once WITHOUT even -ss 0 (that also keyframe-seeks), tries alternate samples after silence, requires separated consistent anchors. Actual corrected alignment passed both cached sections at near-perfect correlation; diagnostics only under .cache, production DB/media unchanged. UI now has Retry render (media only, revision guarded, no review override), explicit durations/counts and vertical download links. No app launch/restart, new download, inference or final render performed. See docs/clip-fixes.md and docs/selection-design.md for current behavior and remaining real quality validation.
